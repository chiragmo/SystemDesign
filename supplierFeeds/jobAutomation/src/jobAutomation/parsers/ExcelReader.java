package jobAutomation.parsers;

protected class ExcelReader implements FileReader {

	protected ExcelReader() {

	}

	@Override
	protected List<HashMap<String,String>> getBatch() {
		/*
		* Follow Regex parsing logic to split the string according
		* Store the column and value in hashmap<column,value>
		*/
	}


}